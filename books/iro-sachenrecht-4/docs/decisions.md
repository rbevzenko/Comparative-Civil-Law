# Журнал решений по книге

Gert Iro, Bürgerliches Recht Band IV: Sachenrecht, 6. Aufl., Verlag Österreich 2016.
Файл: `iro-sachenrecht.pdf`, 333 страницы, sha256 `60b88d466b28995a…`, скан большого разрешения (1777×2659 pt).

| Дата | Вопрос | Решение | Кто | Улики |
|---|---|---|---|---|
| 2026-08-19 | Выходные данные | Титул (f3) и оборот титула (f4). `cite_as` согласован с уже загруженным томом той же серии (S. Dullinger, Schuldrecht AT⁸ Rz 1/7) | агент | «Gert Iro, Bürgerliches Recht Band IV, 6. Auflage, 2016, Verlag Österreich GmbH, Wien» |
| 2026-08-19 | Границы тела | `--pages 25-301`. До f24 — предисловия, оглавление, список сокращений; с f303 — Paragraphenregister и Sachverzeichnis | агент | f25 — «Erster Teil», типографская 1; f303 — «Paragraphenregister» |
| 2026-08-19 | Ветка | A, вопреки подсказке паспорта про сравнение веток. Слой чужой, но чистый (0.3% подозрительных слов), а ветка B на страницах такого размера стоила бы несоразмерно | агент | покрытие растром 100%, но текст извлекается связно; отчёт качества по ветке A прошёл пороги без правок номеров |
| 2026-08-19 | Единица цитирования | Составная и печатается слитно: «1/13» = § 1 Rz 13. Номер параграфа берётся из той же строки, что и номер единицы, а не из колонтитула: там он стоит всегда | агент | оглавление книги устроено так же (колонка «Rz» со значениями «2/26»), внутренние отсылки тоже («vgl Rz 2/29») |
| 2026-08-19 | Номер на внешнем поле | Написан `normalize_margin_numbers.py`. Номер висит на внешнем поле и меняет сторону на развороте: на чётных страницах файла слева (x≈152), на нечётных справа (x≈1540). Режим `margin` его не берёт — там номер обязан быть голым числом; режим `flow` брал только левую половину книги | агент | без нормализации 337 карточек, после неё 687; снято слева 343, справа 346 |
| 2026-08-19 | Метка снятого номера | Номер отделяется от текста неразрывным пробелом, и паттерн единицы требует именно его. Без метки паттерн ловил перенос перекрёстной ссылки: строка «5/79 ff). Weiters …» — это продолжение «(vgl Rz 5/79 ff)», а не начало единицы | агент | до метки: ложная карточка Rz 5/79 на f171 и дубль Rz 10/42; U+00A0 в слое книги не встречается ни разу |
| 2026-08-19 | Иерархия | Написан `parse_toc.py`. Общий `toc_parse.py` дал 2 узла из 291: у оглавления две колонки цифр («Rz» и «Seite»), и на скане они регулярно уезжают в собственную строку, из-за чего заголовок теряет свою страницу. Строки собираются заново по вертикали, правая колонка приклеивается к ближайшему заголовку | агент | 291 узел, привязано к строке тела 287, с точностью до страницы 4 |
| 2026-08-19 | Уровень узла | Часть, параграф и буквенная рубрика различаются маркером — по отступу их не разделить, все три стоят у левого края (212–238 pt). Глубже наоборот: маркер портится распознаванием (римская «I.» читается как «1.»), и уровень надёжнее берётся из отступа | агент | уровни: 4 части, 17 параграфов, 60 буквенных, 99 римских, 81 арабских, 30 буквенных со скобкой |
| 2026-08-19 | Номера сносок | Тот же `fix_footnote_markers.py`, что у Insolvenzordnung: издательство ставит голое число, и вся полоса сносок ложилась одним блоком | агент | было 255 блоков, из них 226 без номера; стало 583 сноски, без номера 40 |
| 2026-08-19 | Регистрация источника в сервисе | Сначала `POST /sources` отбивался 413: PDF 10.6 МБ не проходил предел nginx на тело запроса (по умолчанию 1 МБ). Владелец поднял `client_max_body_size 1024m` в блоке `http`, после чего источник заведён скриптом `create_source.py` из того же `meta.json`, по которому нарезаны карточки. `source_id` = `cee6d949-5cd6-44df-891e-4114fe44f703` | владелец + агент | до правки `HTTP 413 Request Entity Too Large` от nginx/1.18.0, после — 401 на POST без токена, то есть запрос доходит до приложения |
| 2026-08-19 | Загрузка | 687 карточек, режим upsert по `external_id`, модель эмбеддинга `text-embedding-3-small`. Проверка независимая: поиск по фразе из Rz 2/13 возвращает карточку первой, с верным адресом, полной иерархией и разобранными сносками | агент | `GET /sources/{id}/chunks` → total 687 |
| 2026-08-19 | Внешняя проверка адреса | Составной адрес подтверждается сторонним источником в том же корпусе: Bankvertragsrecht IX ссылается на эту книгу как «Iro, SachenR Rz 8/27» — ровно тот адрес, который несёт наша карточка | агент | фрагмент Rn 3/33 Bankvertragsrecht IX, сноска в тексте |

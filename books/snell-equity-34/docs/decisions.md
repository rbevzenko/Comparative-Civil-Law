# Журнал решений по книге

John McGhee QC & Steven Elliott QC (eds.), Snell's Equity, 34th edition, Sweet & Maxwell 2020.
Файл: `snell-equity.pdf`, 1155 страниц, издательский PDF.

| Дата | Вопрос | Решение | Кто | Улики |
|---|---|---|---|---|
| 2026-08-20 | Выходные данные | Титул (f18): «SNELL'S EQUITY, Thirty-Fourth Edition, JOHN MCGHEE QC … STEVEN ELLIOTT QC». Оборот (f2): «Published and typeset in 2020 by Thomson Reuters, trading as Sweet & Maxwell», ISBN 978-0-414-07150-6 | агент | f2, f18 |
| 2026-08-20 | Границы тела | f19–f1116. До f18 оглавление и таблицы, с f1117 Index | агент | f19 — шмуцтитул «PART I—EQUITY AND EQUITIES»; f1117 — «INDEX» |
| 2026-08-20 | Единица цитирования | Номер абзаца «глава-номер», например 3-031. Так книга и цитируется английскими судами и литературой: Snell's Equity, 34th edn, para.3-031. Раздел ряда — глава, нумерация внутри главы своя | агент | 45 рядов, непрерывность 100% |
| 2026-08-20 | Номер стоит на ВНЕШНЕМ поле и меняет сторону | На одной стороне разворота номер — первое слово строки у левого поля (x0≈28 при левом крае полосы 51), на другой — последнее слово у правого (x0≈391 при правом крае полосы 391). Штатный режим `margin` не годится: `pull_margin_tokens` берёт с поля только голое число `\d{1,4}`, а здесь номер составной. Поэтому `scripts/move_margin_numbers.py` переносит правый случай в начало строки, и дальше работает обычный `flow` | агент | 1024 номера перенесено с правого поля, 957 уже стояли первыми |
| 2026-08-20 | Порог поля 380 пунктов | Тем же паттерном набраны перекрёстные ссылки в тексте («see paras 12-034 and 12-035») и строки главного оглавления с отточием. Они лежат ВНУТРИ полосы: у оглавления номер прижат вправо к x≈377, у ссылок в тексте правый край строки 391. Поле начинается за 391. Порог 380 разделяет их чисто | агент | замер по каждой пятой полосе: FIRST x0≈30 (198), LAST x0 350–367 (227, оглавление), LAST x0 385–392 (210, поле) |
| 2026-08-20 | Ложные единицы от перенесённой ссылки | Две строки начинались с номера, потому что перекрёстная ссылка перенеслась: «12-034 and 12-035 that each …» и «13-001 to 13-004. In Cavendish …». Они сняты `scripts/merge_backward_units.py --per-section`: номер, идущий назад внутри своей главы, единицей не является. Флаг `--per-section` добавлен в этом прогоне — без него первая единица каждой главы (13-001 после 12-045) читалась бы как ход назад | агент | 1981 → 1979 карточек, дублей номеров 0, пропусков 0 |
| 2026-08-20 | Знак сноски в середине строки | `pipelib.cluster_lines` приклеивает верхний индекс к строке тела только с краю (`beside`), а здесь знак стоит после запятой в середине фразы и остаётся отдельной строкой из одних цифр — примерно по пять на полосу. Написан `scripts/inline_superscripts.py`: строка-хозяин ищется по вертикальному перекрытию, число возвращается на своё место по x | агент | 7132 знака возвращено на 1063 полосах |
| 2026-08-20 | Знак сноски ломал разбор аппарата | Знак в нижней половине полосы (кегль 6, ниже `bottom_band`) уезжал в блок сносок, читался там как номер сноски и сбивал счётчик `fix_footnote_markers.py` — вся полоса сносок ложилась одним блоком. На f61 так терялось 10 сносок из 12. Поэтому `inline_superscripts.py` забирает знаки и из `note_lines`, но только те, у которых есть строка-хозяин в теле | агент | f61: было 2 сноски (170, 171) → стало 12 (160–171) |
| 2026-08-20 | Знак сноски вместо колонцифры | Колонцифра стоит внизу отдельной строкой в квадратных скобках («[45]», нижний край 0.936 высоты). При штатной полосе 0.12 в ВЕРХНЮЮ полосу попадал знак сноски, уехавший на свою строку («188» на 0.08 высоты), и читался как номер страницы, а сама строка снималась как колонтитул. Полоса сужена до 0.07 | агент | f63 показывал printed_page 188 вместо 47, f64 — 201 вместо 48 |
| 2026-08-20 | Пробелов в текстовом слое нет | Слова разделены только положением символов. На основном тексте порог `pipelib._space_gap` (0.35 медианной ширины символа) работает. На заголовке прописными вразрядку — нет: в «THE NATURE, HISTORY AND COURTS OF EQUITY» медианная ширина 7.2, порог 2.52, межсловный зазор 2.13–2.49, и заголовок склеивался. Написан `scripts/respace_caps.py`, пересобирающий по символам только строки с длинной цепочкой прописных, порог 0.15 | агент | 39 строк пересобрано; «THENATURE,HISTORYANDCOURTSOFEQUITY» → «THE NATURE, HISTORY AND COURTS OF EQUITY» |
| 2026-08-20 | Оглавление главы на её первой полосе | Каждая глава открывается собственным оглавлением с отточием. Точки отточия разделены пробелами, поэтому шаблон мусора `(?:\.\s?){5,}`, а не `\.{6,}` | агент | текст книги 2 458 694 → 2 378 897 символов |
| 2026-08-20 | Иерархия | Узлы зашитого оглавления озаглавлены голыми числами («1-1», «2-2», «3»), и иерархия из них выходила из одного номера главы. Собрана по заголовкам в теле: PART → CHAPTER + название следующей строкой → раздел «N.—». Флаг `--stop '^CONTENTS$'` добавлен в этом прогоне: под названием главы стоит слово CONTENTS, и без оговорки оно приклеивалось к названию | агент | все 1979 карточек с иерархией, например «PART VII—SECURITIES › CHAPTER 45. SURETYSHIP › 6.— REMEDIES OF SURETY: AFTER PAYMENT» |
| 2026-08-20 | Ведущие нули в адресе | `extract.py` хранит номер целым числом, и адрес выходил «para. 9-16». Книга цитируется «para.9-016», и без нулей ссылка не ищется. `scripts/pad_address_numbers.py` дополняет номер нулями в адресе, цитате и `unit_number`; целое `number` и `external_id` не трогаются — по ним считается ряд и идёт идемпотентность | агент | 1978 карточек из 1979 |
| 2026-08-20 | Регистрация и загрузка | Источник заведён как `textbook`, id `a838bc38-083a-4768-a83b-47ecd6f5afed` | агент | `GET /sources/{id}/chunks` |
| 2026-08-20 | Перенос маргинального номера идёт ПОСЛЕ вклейки надстрочных знаков | `inline_superscripts.py` пересортирует слова строки по x0, и номер, уже перенесённый в начало, уезжает обратно на поле. При старом порядке extract.py терял 92 адреса из 1981 | агент | 106 строк с номером последним словом при x0=390.6 > --min-x 380 |
| 2026-08-20 | Отточие в шаблоне мусора ужато | Второй шаблон `(?:\.\s?){2,}\s*\d{1,2}-\d{3}\s*$` снимает 23 строки постраничного оглавления главы и ни одной строки тела | агент | f19–f200: сняты только строки оглавления |
| 2026-08-20 | `--title-on-next` не применяется к разделам с названием на той же строке | Раздел «1.— GENERAL.» короче трёх слов и по старому признаку считался голым номером — к нему прирастал первый абзац его же тела. Признак теперь: после совпадения с уровнем ничего не осталось | агент | 32 узла иерархии из 249 были кусками текста |
| 2026-08-20 | В прогоне иерархии стоит `--drop-trailing-marker` | Знак сноски прирастает к последнему слову заголовка («2.— MORTGAGES OF LAND24») | агент | 8 таких узлов |


## Показатели

Карточек 1979 (45 рядов, по одному на главу), непрерывность 100.0%, покрытие текста 99.1%, пропусков в нумерации 0, дублей номеров 0, карточек без иерархии 0, мёртвых страниц 0, сносок разобрано 1214, блоков без читаемого номера 389. Узлов иерархии 248.

## Что осталось за границей

Index (f1117–1155) не нарезается: это указатель, а не текст с единицами цитирования.

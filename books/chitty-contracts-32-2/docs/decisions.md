# Журнал решений по книге

Chitty on Contracts, 32nd edition, Volume 2 — Specific Contracts, Sweet & Maxwell.
Файл: `chitty-vol2.pdf`, 2453 страницы, выгрузка Westlaw/ProView.

| Дата | Вопрос | Решение | Кто | Улики |
|---|---|---|---|---|
| 2026-08-20 | Профиль | Тот же файл-формат и то же издание, что и `chitty-contracts-32-1`: выгрузка Westlaw/ProView, профиль `westlaw-proview-export-v1` без изменений. Все решения по вёрстке — в журнале первого тома | агент | колонтитул «Chitty on Contracts 32nd Ed., Volume 2 - Specific Contracts» |
| 2026-08-20 | Выходные данные | Из колонтитула выгрузки и знака копирайта «© 2016 Sweet & Maxwell». Титула и оборота титула в выгрузке нет | агент | колонтитул на каждой полосе |
| 2026-08-20 | Границы тела | f24–f2453 (главы 31–45). f1–f23 — постраничное оглавление по главам | агент | f24 — первая единица 31-001 |
| 2026-08-20 | Уровня Part нет | В этом томе структура идёт Volume → Chapter → Section → Sub-section, разбиения на Part нет вовсе. Уровень Part из набора `--level` убран, иначе он подхватывал бы чужие строки | агент | ни одной строки «Part N - » на 2430 полосах |
| 2026-08-20 | Сирота колонтитула в заголовке | Строка колонтитула переносится, и её хвост («1999» от «…Consumer Contracts Regulations») остаётся на полосе сиротой того же кегля, что и заголовок абзаца. Сама строка снимается как мусор, сирота — нет, и она уходила в заголовок впереди его текста: «38-201 1999 Temporal Application of the 1999 Regulations». Паттерн единицы требует прописной после номера, и такие единицы терялись. Добавлен `--head-start`: всё выше первой строки с прописной отбрасывается | агент | пропусков в нумерации 20 → 4, карточек 3861 → 3877 |
| 2026-08-20 | Оставшиеся четыре пропуска | 40-108, 40-109, 40-232, 44-164 в файле отсутствуют вовсе: таких номеров абзацев в выгрузке нет. Это не промах паттерна | агент | поиск по строкам полос — ни одного вхождения |
| 2026-08-20 | Регистрация и загрузка | Источник заведён как `textbook`, id `a3319b17-5f47-45ce-ba27-638adc6c48cf` | агент | `GET /sources/{id}/chunks` |

## Показатели

Карточек 3877 (15 рядов, по одному на главу), непрерывность 99.9%, покрытие текста 99.4%, пропусков в нумерации 4 (все — отсутствующие в самом файле номера), дублей номеров 0, карточек без иерархии 0, сносок разобрано 19035, блоков без читаемого номера 0, карточек со сносками 96%.

## Что осталось за границей

Одна сноска не нашла своего знака в тексте и в карточку не попала.

Постраничное оглавление (f1–f23) не нарезается: это перечень глав, а не текст с единицами цитирования.

# Отложенные доработки сервиса

Здесь то, что решено делать, но не сейчас. Записано, чтобы не потерялось.

## 1. Страница печатного издания — ЗАКРЫТО (2026-08-21)

**Что не так.** В сервис уходят только `page_start`/`page_end` — это номера
страниц ФАЙЛА-источника. Номера страниц печатного издания у карточек есть:
пайплайн читает колонцифру с полосы и кладёт её в `printed_page_start` /
`printed_page_end`, но `upload_projection.py` эти поля не выгружает, а в
схеме чанка их нет вовсе.

**Чем это плохо.** У Snell's Equity para. 3-030 сервис показывал «страница
60–61», тогда как в книге это страницы 44–45. Кто процитирует такой номер —
процитирует не ту страницу.

**Что сделано пока.** Номер страницы убран из выдачи коннектора и с
публичной страницы цитаты; на служебных страницах он назван «Страницы PDF».
В правилах коннектора запрещено выносить номер страницы в сноску.

**Сделано 2026-08-20.** Пункты 1–3 закрыты: поля в `DEFAULT_FIELDS` и в
`upload_corpus.py`, колонки `printed_page_start`/`printed_page_end` в модели
и миграция 0003, поля в `ChunkCreate`/`ChunkRead` и запись в сервисе чанков,
показ в коннекторе («стр. книги»), на публичной странице цитаты и отдельной
колонкой в служебных таблицах. Правило коннектора переписано: страница
берётся только из этого поля и по файлу не восстанавливается.

**Развёрнуто 2026-08-21.** Миграция 0003 применена, колонки в базе есть.
Но у всех 116 539 чанков они пустые: корпус грузился в сервис ДО миграции,
и страницы, прочитанные пайплайном, просто некуда было положить.

**Дозалито 2026-08-21.** Не перезаливкой: через `PATCH /sources/{id}/chunks`
(пункт 10) и `scripts/patch_printed_pages.py` — правятся только реквизиты,
эмбеддинги не считались вовсе. **59 632 карточки в 72 книгах получили номер
печатной страницы**; сопоставились все 93 книги, ненайденных `external_id`
ноль, пропусков ноль.

У 21 книги колонцифры нет ни на одной полосе (выгрузки Westlaw, e-book'и) —
24 584 карточки, там править нечего, и `null` у них означает именно «в файле
номера нет». Ещё около 32 тысяч чанков — тридцать один источник из пункта 7,
которого в репозитории нет вовсе: у них печатной страницы не будет никогда.

Проверено по всей цепочке на карточке, с которой пункт начинался: Snell's
Equity para. 3-030 — страница файла 60, **страницы книги 44–45**, ровно как
в издании. Публичная страница цитаты показывает «Страница 44–45», коннектор
отдаёт «стр. книги: 103–104» у para. 5-014.

Сборка текста (пункт 3) остаётся отдельной работой и по-прежнему требует
перезаливки с эмбеддингами: PATCH текст не принимает намеренно.

Колонцифра прочитана у 33 источников из 38. Полностью нет типографской
пагинации у выгрузок Westlaw (оба тома Chitty, Goff & Jones), у Treitel,
Burrows (English Private Law и Obligations) и Davies — там показывать нечего,
и это надо отличать от «не выгрузили». Частично прочитана у бельгийских
книг (48–50%), Vansweevelt (90%), Turner (74%), MacKenzie & Phillips (95%),
Sparkes (96%), Finch (97%).

## 2. Слой «судебная практика» (2026-08-20)

Правила коннектора описывают три слоя: догматика, комментарии, судебная
практика. Третьего слоя в корпусе нет ни по одной юрисдикции; `source_type`
допускает только `textbook` и `commentary` (Literal в схеме плюс
CheckConstraint в таблице). Когда источники практики появятся, понадобится
третье значение и миграция ограничения.

## 3. Текст карточек в старых источниках — СОБРАН, ждёт перезаливки (2026-08-20)

**Что не так.** В карточку текст попадает так, как стоял на полосе: с
переносами строк по ширине полосы и с переносами СЛОВ по дефису
(«Gläu-\nbigers»), а слова разделены неразрывным пробелом U+00A0. На экране
это выглядит рваным, а поиск по фразе не находит того, что читается глазом.

**Что сделано.** `scripts/reflow_card_text.py` сращивает перенос слова,
склеивает строки абзаца и приводит пробелы к обычным. Висячий дефис
(«sitten- oder») сохраняется по списку союзов, конец абзаца и заголовок
распознаются по длине строки. 2026-08-20 добавлено сохранение перечислений:
строка, начинающаяся с «(a)», «(iii)», «(2)», «1.» или маркера списка,
остаётся отдельной — иначе подпункты нормы слипаются в сплошной абзац.
Проверено на Megarry & Wade 17-081.

**Сделано 2026-08-20.** Скрипт прогнан по всем 38 книгам: 46 993 карточки,
собрано 45 141. Заодно вылезли три вещи.

* Мягкий перенос U+00AD (1557 карточек у Vansweevelt, ещё 4 в других книгах)
  раньше не снимался вовсе: висячим дефисом он не бывает, ставит его только
  вёрстка, — теперь снимается вместе с переводом строки.
* Узлы иерархии — тот же набранный текст, и в 41 узле оставался тот же
  мягкий перенос. Иерархия тоже собирается.
* **Скрипт нельзя было прогонять дважды.** Конец абзаца распознаётся по
  длине строки относительно остальных в карточке; в уже собранном тексте
  строка — это целый абзац, и соседние абзацы склеиваются между собой. На
  Megarry & Wade второй прогон менял 714 карточек из 2929. Теперь карточка
  метится ключом `text_reflowed` и второй раз пропускается; `--again`
  снимает защиту.

**Что осталось.** Перезалить всё — одним проходом вместе с пунктом 1
(страница печатного издания), чтобы не считать эмбеддинги дважды. Ждёт
развёртывания сервиса. Готово к запуску: `scripts/reupload_all.py`
(38 книг, 57 278 карточек) и `scripts/reflow_service_source.py` для
тридцати одного источника, которого в репозитории нет вовсе (31 912 чанков,
см. пункт 7). Итого 89 190 чанков, порядка пяти-шести часов.

## 4. Испорченные узлы иерархии у Snell's Equity — СДЕЛАНО (2026-08-20)

**Что было.** 21 узел иерархии из 250 содержал кусок оглавления и хвост
абзаца: «10.— Equity Looks on as Done That Which Ought to be Done . . 5-015
statement of a broad theme which underlies…».

**Причин оказалось четыре, а не две.**

1. Продолжение заголовка искалось ЧЕРЕЗ строку — ошибка на единицу в
   `hierarchy_from_headings.py` (найдена на Sparkes, A New Land Law).
2. Строка постраничного оглавления главы с редким отточием («. . » вместо
   «. . . . . .») не подошла под шаблон мусора и стала заголовком. В профиль
   Snell добавлен второй шаблон `(?:\.\s?){2,}\s*\d{1,2}-\d{3}\s*$` —
   он снимает ровно 23 строки оглавления и ни одной строки тела.
3. `--title-on-next` считал голым номером всё, что короче трёх слов, и
   раздел «1.— GENERAL.» получал следующей строкой первый абзац своего же
   тела. Теперь признак — то, что ПОСЛЕ совпадения с уровнем ничего не
   осталось, а не длина строки.
4. Знак сноски прирастал к последнему слову («2.— MORTGAGES OF LAND24»).
   Флаг `--drop-trailing-marker` был, но в прогоне не стоял.

**Заодно найдена потеря 90 адресов, и она оказалась глубже, чем думалось.**
Snell печатает номер абзаца на ВНЕШНЕМ поле, и на чётных полосах он —
последнее слово строки. `move_margin_numbers.py` переносит его в начало,
оставляя слову исходную геометрию: номер стоит первым в списке, а x0 у него
от правого поля. Дальше строку пересобирали ДВА скрипта, и оба сортировали
слова по x0 — `inline_superscripts.rebuild()` и `respace_caps`. Номер улетал
обратно в конец, и `extract.py` переставал видеть адрес. Через `rebuild`
проходят только строки, получившие сноску, — отсюда ровно 106 испорченных из
1024 перенесённых.

Исправлено в трёх местах, каждое закрывает свою грань:

1. `rebuild()` больше не переставляет слова: порядок по x0 нужен только
   чтобы найти соседа для склейки маркера, а текст собирается в том
   порядке, в каком слова лежат в строке. Маркер при этом вливается в
   слово-хозяина насовсем и отдельным словом не числится — иначе список
   слов и текст строки расходятся, а следующий шаг смотрит именно на
   последнее слово (на этом первая попытка починки и споткнулась: перенос
   дал 932 вместо 1024).
2. `respace_caps` пропускает строки с переставленными словами.
3. Перенос номеров идёт ПОСЛЕДНИМ шагом, после всего, что пересобирает
   строки.

**Последние 14 узлов** — строки оглавления, где отточие ужалось до ОДНОЙ
точки («…Clean Hands . 5-010»). Ловятся по пробелу перед точкой; проверено
по всей книге, текста среди них нет. Применить шаблон помог новый
`scripts/drop_junk_lines.py`: мусорные шаблоны профиля срабатывают при
наборе КЭША, и `pages_digital --skip-done` их заново не спрашивает, так что
шаблон, добавленный после набора кэша, не действует. Полный переразбор ради
одной строки — сорок минут, скрипт снимает её за секунду.

**Итог.** Книга пересобрана и перезалита: 1979 карточек (было 1889),
непрерывность 100% против 95.5%, пропусков 0 против 76, дублей 0, узлов
иерархии 247 и ни одного засорённого.

## 5. Перепарсить книги с фантомными пробелами — СДЕЛАНО (2026-08-20)

`pipelib.chars_to_lines` теперь снимает пробельные глифы, нарисованные
поверх соседней буквы (см. `drop_phantom_spaces`). До этой правки такие
книги разрывались по лигатурам и по первой букве абзаца: «Th e»,
«signifi cance», «aft er», «L and Law». Раньше это чинилось постфактум
регуляркой `fix_broken_ligatures.py`, которая ловит не всё: разрыв по «ft»
и оторванная первая буква ей не по зубам.

Аудит уже загруженных книг (`\b[A-Za-z]*(?:Th|ffi|ffl|fi|fl) [a-z]` по
`output/cards.jsonl`): чинить нужно только **mackenzie-phillips-land-law** —
83 карточки из 1138. У chitty-vol1 одна карточка, у остальных английских и
австрийских книг ноль: там `fix_broken_ligatures.py` отработал.

Правило «оторванная первая буква» отдельным аудитом не измеряется:
`(?<![A-Za-z])[B-HJ-Z] [a-z]` в этих книгах почти целиком ложные
срабатывания («equity thinks B should be owner», «X v Y»).

**Сделано 2026-08-20.** `mackenzie-phillips-land-law` перепарсена с чистого
кеша (f40–f619) и перезалита: 1138 карточек, метрики те же (непрерывность
99.6%, покрытие 99.5%), разрывов по лигатурам ноль. Пункт закрыт.

## 6. Двенадцать книг лежали в репозитории без cards.jsonl.gz (2026-08-20)

`books/*/output/cards.jsonl` в `.gitignore`, а в репозиторий кладётся сжатый
`cards.jsonl.gz` — но у двенадцати книг его не положили, и единственная копия
карточек была рабочей. Прогон сборки текста затёр её без возможности отката.

Текст восстановлен из сервиса по `external_id` (в корпусе лежала версия до
сборки), .gz закоммичен для всех тридцати восьми книг. Двух карточек в
сервисе не нашлось — по одной у Davies и Hanbury & Martin: в них остался
текст после двойной сборки, то есть пара абзацев склеена. При следующей
пересборке этих книг разойдётся само.

Чтобы это не повторилось: `cards.jsonl.gz` кладётся в тот же коммит, что и
отчёт о качестве, — до любой выгрузки в сервис.

## 7. Тридцать один источник существует только в сервисе (2026-08-20)

Австрийский корпус заводился до нынешнего пайплайна: Klang Grosskommentar
(16 томов), Bankvertragsrecht (7 томов), Bydlinski, Welser, Rummel и прочие
— 31 источник, 31 912 чанков. Каталогов в `books/` у них нет, карточек тоже:
восстановить их нечем, PDF в контейнере не сохранились.

**Чем это плохо.** Всё, что делается пайплайном, мимо них проходит. Собрать
текст (пункт 3) им можно только через сервис, а страницу печатного издания
(пункт 1) — уже никак: колонцифра была прочитана при нарезке и никуда не
записана.

**Что сделано.** `scripts/reflow_service_source.py` читает чанки источника
постранично, собирает текст, иерархию и сноски и пишет обычный
`upload.json` — дальше `upload_corpus.py` грузит его по `external_id`.
Чтобы это работало, у списка чанков появился `?with_footnotes=true`: приём
заменяет набор сносок целиком, и чанк, посланный без них, свои сноски
теряет, а поштучный `GET /chunks/{id}` на тридцать тысяч чанков — это
тридцать тысяч запросов.

**Что нужно сделать когда-нибудь.** Если найдутся исходные PDF — завести эти
книги в `books/` как все прочие и перенарезать: тогда у них появятся и
колонцифра, и сноски по нынешним правилам, и воспроизводимость.

## 8. Рубрика «Comparative law» собрана — 55 источников, 27 349 фрагментов (2026-08-21)

Наднациональный слой корпуса: сравнительно-правовые исследования, серия
Common Core, оксфордские справочники, монографии по отдельным институтам.
Все 55 книг из папки владельца обработаны и залиты.

**Название.** Рубрика заводилась как `DCFR`, но собственно текстов Draft Common Frame
of Reference в
ней почти нет: это сравнительно-правовая литература, а наднациональные
проекты (DCFR, PECL, Common Core) в ней лишь один из сюжетов. 2026-08-21
переименована — сначала в `Comparative literature`, затем, в тот же день,
в `Comparative law` — во всех 55 источниках сервиса,
в meta.json и профилях книг, в правилах коннектора и в списке юрисдикций
на форме. Идентификаторы профилей (`dcfr-…-v1`) оставлены как есть: они
записаны в происхождении уже выгруженных карточек.

**Что осталось по рубрике.**

1. **Свой `source_type`.** Сервис знает только `commentary` и `textbook`.
   Сравнительное исследование — ни то ни другое; всё заведено как
   `textbook` за неимением лучшего. Нужен третий тип (`monograph`) —
   одной строкой в `SourceType` и миграцией не требуется, поле строковое
   в схеме, но `Literal` его не пустит. Делать вместе с пунктом 2
   (слой судебной практики), чтобы не разворачивать сервис дважды.

2. **«Comparative law» нет в выпадающем списке юрисдикций** на форме добавления
   источника. В базе строка свободная, источники завелись, но список в
   `source_form.html` подтянется только после развёртывания.

3. **Две книги в папке — дубликаты.** `Smiths_Elgar_Encyclopedia_of_
   Comparative_Law.pdf` — тот же Смитс, что и `Smits_Encyclopedia…`,
   другой скан. `Zimmermann_Oxford_Handbook_of_Comparative_Law_altfile.
   pdf` — тот же оксфордский справочник, что и `Reimann_Zimmermann…`.
   Оба пропущены намеренно, чтобы не задваивать источник.

4. **Три книги с оговоркой о качестве распознавания.** У Nossek
   («Grrmdregisterrec/11»), Zimmermann/Whittaker Good Faith («CONTRACT
   I.AW») и Palmer/Bussani распознавание среднее: для поиска по смыслу
   годится, для ДОСЛОВНОЙ цитаты — нет. В журналах решений это записано;
   стоит подумать, не пометить ли такие источники в сервисе отдельно,
   чтобы коннектор не выдавал из них цитаты как дословные.

5. **Иерархии нет у Palmer/Bussani.** Единственная книга рубрики без
   дерева: при разбросе кегля у скана обычный текст неотличим от
   заголовка, и дерево наполнилось бы мусором.

**Что рубрика дала пайплайну.** `complit_pipeline.sh` (общий прогон, падает
на дублях адреса), `section_from_hierarchy.py` (номер раздела из иерархии,
чередующиеся ряды нумерации, сквозная перенумерация),
`hierarchy_from_running_head.py` (иерархия из колонтитула),
`split_at_page_markers.py`, `drop_junk_lines.py`, два новых режима
`rebuild_notes.py` (`--marker-line` и необязательная точка после номера),
вычистка управляющих знаков в `reflow_card_text.py`.

## 9. Две переименованные юрисдикции (2026-08-21)

**`DCFR` → `Comparative law`.** Сначала переименовали в `Comparative
literature`, но по-английски это значит сравнительное ЛИТЕРАТУРОВЕДЕНИЕ,
а не правоведение: человеку контекст очевиден, а модель, читающая поле
вслепую, поняла бы иначе. В тот же день исправлено на `Comparative law`.
Рубрика заводилась под рабочим
именем DCFR, но собственно текстов Draft Common Frame of Reference в ней
почти нет: это сравнительно-правовая литература, а наднациональные проекты
(DCFR, PECL, Common Core) — лишь один из её сюжетов.

**`UK` → `EN`.** Расхождение заметил владелец: список на форме предлагал
`EN`, а все 26 английских источников были заведены как `UK`. Сведено к `EN`.

**Почему переименование безопасно.** У чанка колонки `jurisdiction` НЕТ —
она есть только у источника (`Source.jurisdiction`, обычный индексированный
Text). Поиск join-ит таблицу источников и фильтрует по ней
(`search.py:28`), эмбеддинги и постоянные ссылки на фрагменты значения
юрисдикции не содержат. Переименование — это UPDATE 55 и 26 строк в одной
таблице, ничего каскадного.

**Что поправлено вместе с данными:** `meta.json` и профили всех книг,
журналы решений, правила коннектора (`app/mcp/server.py` — словарь кодов,
кластеры, правило об обязательном просмотре сравнительно-правового слоя,
описание фильтра `search_corpus`, перечень пустых юрисдикций в
`corpus_stats`), списки на формах добавления и правки источника.

**Что НЕ трогалось намеренно:** идентификаторы профилей (`dcfr-…-v1`). Они
записаны в поле `profile` уже нарезанных карточек, и переименование
рассинхронизировало бы репозиторий с выгруженным.

**Осталось:** развернуть сервис, иначе списки на формах и правила
коннектора останутся прежними — в базе переименование уже произошло.
**Бэкап (2026-08-20).** Пока PDF не нашлись, единственная защита этих
тридцати одного источника — регулярная копия: `scripts/backup.py`
выгружает их из сервиса по API вместе со сносками, `pg_dump` на хосте
берёт их же вместе с векторами. См. `docs/backup.md`.

## 10. Правка чанков без пересчёта эмбеддингов и снятие Caddy (2026-08-21)

**`PATCH /sources/{id}/chunks`.** Пакетная правка реквизитов чанков с
адресацией по `external_id` — тому же ключу, что и загрузка. Отвечает
`{updated, unchanged, missing}`. Есть и точечный `PATCH /chunks/{id}`.

Правятся `citation`, `point_number`, `hierarchy`, `page_start`, `page_end`,
`printed_page_start`, `printed_page_end`. Применяются только присланные
поля; «не прислали» и «прислали `null`» различаются по `model_fields_set`,
иначе обнулить страницу было бы нечем. Схема с `extra="forbid"`: опечатка в
имени поля — ошибка, а не тихо прошедший запрос, который ничего не изменил.

**Текста в PATCH нет намеренно.** Эмбеддинги считает внешний пайплайн, а не
сервис. Правка текста через PATCH оставила бы старый вектор при новом
тексте: фрагмент находился бы поиском по тому, чего в нём уже нет. Текст
меняется единственным способом — полной перезаливкой через POST, вместе с
вектором.

`updated` и `unchanged` разделены не для красоты: при дозаливке второе
число показывает, сколько карточек уже несут нужные значения, и по нему
видно, что повторный прогон ничего не портит. `updated_at` у совпавших
карточек не трогается — сравнение со старым значением стоит перед
присваиванием, иначе `onupdate` пометил бы правленым весь корпус.

`missing` возвращается, а не превращается в 404 на весь пакет: файл
нарезки может содержать карточки, которые в сервис не грузились, и ронять
из-за них правку остальных незачем. Но и молчать нельзя — иначе
несовпадение ключей выглядит как успешная правка.

**`scripts/patch_printed_pages.py`.** Клиент к этому маршруту.
`source_id` берётся из `books/<id>/source_id.txt`, а у книг, залитых до
появления этого файла (38 из 93), — сличением заголовка и авторов с
каталогом сервиса; найденный id тут же записывается в `source_id.txt`,
чтобы следующий прогон сличением не занимался. Авторы в ключе нужны из-за
двух разных книг с заголовком «Contract Law» (Макендрик и Тёрнер), а
пунктуация из ключа выброшена из-за «Band I.» против «Band I:» у Козиоля.
Карточки читаются из `output/upload.json`, а если его нет (он в
`.gitignore`) — из `cards.jsonl.gz`, который в репозитории лежит.

**Развёрнуто 2026-08-21.** Первая попытка не удалась: пересборка была
запущена ДО `git pull`, собрался старый файл, и compose снова полез
поднимать caddy — «failed to bind host port 0.0.0.0:443/tcp: address
already in use». Это же и есть окончательное доказательство, что caddy на
443 никогда не работал. После `git pull` и `docker compose up -d --build
--remove-orphans` поднялись два контейнера, api и db.

**Caddy убран из `docker-compose.yml`.** 443 на хосте держит системный
nginx: он терминирует TLS, хранит сертификат и проксирует на
`127.0.0.1:8000`. Два сервиса этот порт не делят — caddy либо не
поднимался («address already in use»), либо поднимался вхолостую, и
проверка это подтверждает: ответ на 443 приходит с заголовком
`server: nginx/1.18.0 (Ubuntu)`. Удалены сервис, `Caddyfile` и тома
`caddy_data` / `caddy_config`; переменная `MCP_DOMAIN` осталась — она
нужна не сертификату, а сверке заголовка `Host` в MCP SDK.

При развёртывании: `docker compose up -d --build --remove-orphans` —
иначе контейнер caddy останется работать сам по себе. Тома от него можно
снять потом: `docker volume rm comparative-civil-law_caddy_data
comparative-civil-law_caddy_config`.

## 11. Рубрика «DCFR» (2026-08-21)

Вторая сквозная рубрика корпуса. От «Comparative law» отличается предметом:
там литература О правопорядках, здесь — сами наднациональные модельные
своды и постатейный комментарий к ним. В правилах коннектора разведены
явно, обе объявлены обязательными к просмотру по любому страновому вопросу.

**Чем DCFR ценен именно для страновых вопросов.** Национальные заметки
разобраны по странам и часто прямо называют норму нужного правопорядка:
«GERMANY: CC § 305c(2)… AUSTRIA, CC § 915…». Это записано в правило
коннектора, иначе слой выглядел бы как отвлечённая теория.

**Что дала рубрика пайплайну.**

* `scripts/split_article_commentary.py` — дробление карточки-статьи по
  ПЕЧАТНЫМ делениям: правило, буквенные подзаголовки комментария,
  нумерованные национальные заметки. Заведён потому, что у полного издания
  DCFR комментарий к одной статье доходит до девяноста полос: две трети
  текста тома легло бы в карточки длиннее 18 000 знаков, а эмбеддер видит
  только первые 18 000.
* `scripts/normalize_article_address.py` — один адрес в трёх видах
  («X. –1:101», «X.– 3:101», «X. – 2:102») сводится к одному.

**Три грабли, на которые наступили.**

1. **Номер статьи DCFR — не число.** Пайплайн приводит номер единицы к
   целому, и «I.–1:101» стало 101. 1977 карточек сели на 110 ключей: статья
   101 есть в каждой главе. Лечится составным адресом: книга с главой в
   РАЗДЕЛ, номер статьи — в единицу. Та же машинка, что даёт «§ 812 Rz 45».
2. **Полоса колонтитула меряется по НИЖНЕМУ краю строки.** У томов PEL
   колонтитул слово в слово повторяет заголовок статьи и отличается только
   кеглем; стоит он на 0.0525 высоты, но низ приходится на 0.065, и в полосу
   0.06 не попадал — каждая лицевая полоса открывала ложную статью. И полоса
   эта СВОЯ У КАЖДОГО ТОМА: 0.065 у Lease of Goods, 0.0907 у Benevolent
   Intervention и Service Contracts.
3. **Буква комментария неотличима от иллюстрации по виду строки.** В
   иллюстрациях DCFR стороны названы буквами: «B. After 4 years A purports
   to terminate…». Отличает только кегль: подзаголовок 14, тело 12.

**Порог непрерывности к DCFR неприменим и это не отговорка.** Своды
нумеруют статьи как «раздел плюс порядковый»: за 109 идёт 201, за 202 —
301. Метрика считает это дырой. В главном томе все 19 дыр проверены
поштучно — каждая на границе раздела; по комментариям 99.8%, по заметкам
99.7%, покрытие текста 99.86%. Выгрузка сделана с `--force`, разбор записан
в журнале решений книги.

**Что осталось по рубрике.**

1. **Вступительная статья и раздел Principles полного издания** (полосы
   6–106) не нарезаны. У них своя единица — нумерованный абзац «Intr. 1»,
   и нужен отдельный профиль. Это изложение целей и основополагающих
   принципов DCFR самими редакторами, на него ссылаются.
2. **Краткое издание (`DCFR.pdf`) не режется вовсе** — всё его содержимое
   есть в полном: и статьи, и вступление, и Principles.
3. **Четыре тома — сканы** (PECL Ландо, PEICL, PEL Sales, PEL Unjustified
   Enrichment), 1726 полос. Нужен OCR, и время прогона надо посчитать до
   запуска, а не после.


## 12. Франция (FR): что выяснилось на первых книгах

Рубрика FR заведена, в правилах коннектора она уже есть. Ниже — то, что
стоило крови и пригодится на следующих томах.

**Французские книги бывают трёх вёрсток, и профиль у каждой свой.**

*Бумажная (LGDJ, Малори).* Тело 10.0, «petits caractères» 8.5, сноски 8.0
внизу полосы, знаки надстрочные 4.7/5.6. Кегль сноски и кегль мелкого
шрифта различаются на полпункта — этого хватает, но полосу `bottom_band`
надо опускать почти до нуля и полагаться на кегль: блок сносок начинается
где угодно, хоть на трети высоты. Колонцифра сидит в колонтитуле.

*Электронная Dalloz, вёрстка Nord Compo (Терре, «Обязательства»).* Тело
13.5, аппарат 11.5 на ОТДЕЛЬНЫХ полосах, по десять сносок на полосу, связь
через ссылки-аннотации. Полосы аппарата перемежаются с телом, диапазоном не
отсекаются, и отделяются по модальному кеглю полосы.

*Электронная Dalloz, сборка calibre (Реми-Кабрийак, Терре «Les biens»,
Флур).* Тело 15.0 или 14.4, по ОДНОМУ примечанию на полосу. У Флура полосы
аппарата набраны кеглем 23.8 — КРУПНЕЕ тела, и отделять их по кеглю нельзя;
там работает порог по объёму текста.

**Три грабли, на которые наступали по очереди.**

1. Паттерн номера пункта нельзя писать как «цифры, пробел, заглавная». У
   Малори каждый шестой пункт начинается цифровой рубрикой («130. 1º)»), у
   Терре — строчной со скобкой («22 a)», «176 α)»), встречаются Œ,
   кавычка-ёлочка и многоточие. Каждый недосмотр стоит от 250 до 580
   потерянных номеров.
2. Разрядность номера. У Малори по обязательствам максимум 947 и четвёртая
   цифра ловит годы; у него же по специальным договорам максимум 1269, и
   трёхзначный паттерн обрывает книгу на n° 999 одной карточкой в 144 тысячи
   знаков. Смотреть последний номер книги ДО прогона.
3. Дыры в нумерации бывают печатными. Малори по обеспечительным сделкам и по специальным
   договорам сам печатает «24-99. réservés.»: 350 и 488 номеров из книги
   изъяты автором. Непрерывность падает до 0.56, и выгружать приходится
   через `--force`. Проверка простая и обязательная: собрать из текста все
   диапазоны «N-M. réservés» и убедиться, что каждая дыра попадает в них.

**Хвост книги обрезать до нарезки.** Указатели, таблица соответствия статей
и оглавление иначе целиком уезжают в последнюю карточку.

**Колонцифра электронного издания — не страница книги.** У Терре внизу
полосы стоит число, но это нумерация файла: 3095 полос против примерно 1900
печатных страниц. Хранить её нельзя; читать — нужно, иначе строка попадёт в
текст. Для этого есть `scripts/blank_printed_pages.py`.

## 13. Германия: что сделать после Франции (поставлено пользователем 2026-08-21)

Папка: `1iGOsfAOtBluz1zV-4u43XBCipVLO8XSV`. Пользователь предупреждает, что она
самая сложная, и просит:

1. **Проверить папку на повторы** до начала работы — одна и та же книга может
   лежать в двух видах (том целиком и отдельные части, скан и цифровая копия).
2. **Разобраться с комментариями к ГГУ.** Их будет много, и они разные:
   Staudinger, MüKo, Soergel, Palandt/Grüneberg, BeckOK, Erman. У каждого своя
   единица цитирования и свой способ ссылки; прежде чем резать, установить,
   что это за комментарий и как на него принято ссылаться.
3. **Учесть, что часть литературы написана ДО реформы обязательственного права**
   (Schuldrechtsmodernisierung, 2002). Такие книги комментируют старую редакцию
   ГГУ, и выдавать их за действующее право нельзя.

### Правило коннектора — СДЕЛАНО (п. 6 инструкции, коммит d707241)

Пользователь формулирует его так и требует, чтобы оно работало НЕ ТОЛЬКО для
Германии, а для всех юрисдикций:

* знание модели о **норме**, привязанное к году редакции нормы, имеет приоритет
  перед доктринальным текстом;
* знание модели о **судебной практике**, когда дата судебного акта известна
  точно, имеет приоритет над всем остальным — над догмой, комментарием,
  учебником.

То есть: если источник корпуса издан до реформы или до поворотного решения, его
утверждение о действующем праве считается устаревшим, и об этом надо сказать
прямо, а не пересказывать книгу как действующее право. Год издания у каждого
источника в корпусе есть, так что правило исполнимо.

Написано раньше срока — заодно с деплоем эндпойнта `DELETE /sources/{id}`,
чтобы не гонять деплой дважды. Формулировка легла в п. 6 инструкции
`app/mcp/server.py`. Два уточнения, которых в постановке не было, но без
которых правило опасно:

* приоритет включается только от ТВЁРДОГО знания с датой; при неуверенности
  опорой остаётся корпус. Иначе правило превращается в разрешение возражать
  книге по памяти;
* запрет п. 2 сочинять текст нормы, номер дела и дату акта не отменяется
  ничем — приоритет даёт право поправить источник, а не выдумать основание.

Чтобы правилу было на что опереться, в выдачу фрагмента добавлена строка
«год издания»: в поле «цитата» год стоит не у всякой книги (Gorlier — без
года вовсе).
### Опись немецкой папки (файлы скачаны и вскрыты, 2026-08-21)

137 файлов, 5,6 ГБ, **109 410 страниц**. Для сравнения: вся Франция — 31 книга
и 21 872 карточки. Немецкая папка примерно впятеро больше всего, что уже
сделано, и одним заходом не берётся.

**Побайтные копии — 6 пар, подтверждено sha256.** Часть переименована по-русски,
и по имени они выглядят разными книгами:

| | |
|---|---|
| Looschelders, Schuldrecht. Allgemeiner Teil | Looschelders_Schuldrecht._Allgemeiner_Teil |
| **Behm**, Berger, Вещное право | **Brehm**_Berger_Sachenrecht |
| Stadler, Allgemeiner Teil des BGB | Stadler_Allgemeiner_Teil_des_BGB |
| Wolf, Вещное право 21 Aufl | Wolf_Sachenrecht_21_Aufl |
| Staudingers_Kommentar_zum_Burgerlichen_Gesetzbuch | Eckpfeiler_Des_Zivilrechts_Von_Staudingers… |
| Harke, Allgemeines Schuldrecht | Harke_Allgemeines_Schuldrecht |

Ещё пять пар не совпадают побайтно, но имеют одинаковое число страниц и
одинаковый текст на выборке: Boemke/Ulrici (481), Leenen/Häublein (712),
Brox/Walker AT (451), Fikentscher/Heinemann (1142), Brox/Walker Schuldrecht
(575). Единственная пара с настоящим расхождением — Wieling Sachenrecht:
529 против 528 страниц.

**Четыре разных типа файла — четыре разных конвейера.** Это главный вывод
описи: папка неоднородна, и общего профиля у неё быть не может.


**beck-online (MüKo и др.)** — 26 файл(ов), 12940 стр.

| стр. | файл |
|---:|---|
| 180 | 104-113 ГГУ (Мюнхенский комментарий).pdf |
| 1105 | 104-144 ГГУ (Мюнхенский комментарий).pdf |
| 218 | 116-119 ГГУ (Мюнхенский комментарий)-2.pdf |
| 137 | 120-124 ГГУ (Мюнхенский комментарий).pdf |
| 154 | 125-129 ГГУ (Мюнхенский комментарий).pdf |
| 416 | 130-144 ГГУ (Мюнхенский комментарий).pdf |
| 171 | 145-150 ГГУ (Мюнхенский комментарий).pdf |
| 1150 | 145-185 ГГУ (Мюнхенский комментарий).pdf |
| 163 | 151-159 ГГУ (Мюнхенский комментарий).pdf |
| 816 | 160-185 ГГУ (Мюнхенский комментарий).pdf |
| 546 | 186-218 ГГУ (Мюнхенский комментарий).pdf |
| 88 | 227-240 ГГУ (Мюнхский комментарий).pdf |
| 1222 | 241-249 ГГУ (Мюнхенский комментарий).pdf |
| 535 | 250-272 ГГУ (Мюнхенский комментарий).pdf |
| 863 | 273-292 ГГУ (Мюнхенский комментарий).pdf |
| 299 | 293-306а ГГУ (Мюнхенский комментарий).pdf |
| 853 | 307-310 ГГУ (Мюнхенский комментарий)-2.pdf |
| 299 | 311-312 ГГУ (Мюнхенский комментарий).pdf |
| 751 | 313-326 ГГУ (Мюнхенский комментарий) 2.pdf |
| 237 | 327-327p ГГУ (Мюнхенский комментарий).pdf |
| 169 | 387-397 ГГУ (Мюнхенский комментарий).pdf |
| 253 | 516-534 ГГУ (Мюнхенский комментарий).pdf |
| 502 | 985-1011 ГГУ (Мюнхенский комментарий).pdf |
| 413 | 986-1011 ГГУ (Мюнхенский комментарий).pdf |
| 1375 | Baur Sturner Sachenrecht 18th edition.pdf |
| 25 | Vorbemerkung Vor 116.pdf |

**Staudinger онлайн** — 19 файл(ов), 6213 стр.

| стр. | файл |
|---:|---|
| 1229 | 104-144 Staudinger.pdf |
| 523 | 145-185 BGB Staudinger.pdf |
| 379 | 186-218 BGB Staudinger.pdf |
| 1556 | 241-254 BGB Staudinger.pdf |
| 329 | 255-276 BGB Staudinger.pdf |
| 481 | 277-292 BGB Staudinger.pdf |
| 332 | 320-326 ГГУ (Штаудингер).pdf |
| 262 | 387-397 BGB Staudinger.pdf |
| 415 | 433-443 BGB Staudinger .pdf |
| 299 | 516-534 BGB Staudinger.pdf |
| 56 | STAUDINGER_Mietrecht_1.pdf |
| 81 | STAUDINGER_Mietrecht_2.pdf |
| 18 | StaudingerKern_2022_BGB_Sache.pdf |
| 40 | Vorbem zu §§ 116-124 .pdf |
| 72 | Vorbemerkungen zu §§ 104 ff.pdf |
| 32 | Vorbemerkungen zu §§ 145-156.pdf |
| 24 | Vorbemerkungen zu §§ 158-163.pdf |
| 45 | Vorbemerkungen zu §§ 164-181.pdf |
| 40 | Vorbemerkungen zu §§ 182-185.pdf |

**Großkommentare der Praxis (ZPO/InsO)** — 8 файл(ов), 8483 стр.

| стр. | файл |
|---:|---|
| 782 | Insolvenzordnung Band 2 §§ 35-55.pdf |
| 916 | Insolvenzordnung Band 4 §§ 103-128 (Giesen, Jacoby).pdf |
| 915 | Insolvenzordnung Band 8 §§ 286-334 (Foerste, Windel, Preuss).pdf |
| 1128 | ZPO Kommentar Band 2 §§ 50-127a.pdf |
| 1300 | ZPO Kommentar Band 3 §§ 128-252.pdf |
| 1372 | ZPO Kommentar Band 4 §§ 253-299a.pdf |
| 936 | ZPO Kommentar Band 5 §§ 300-354.pdf |
| 1134 | ZPO Kommentar Band 7 §§ 511-591.pdf |

**книги (сканы и e-book)** — 84 файл(ов), 81774 стр.

| стр. | файл |
|---:|---|
| 2992 | Abramenko, Dickersbach, Lützenkirchen, Mietrecht. Kommentar.pdf |
| 4444 | Aktiengesetz Kommentar (2020).pdf |
| 451 | Allgemeiner Teil des BGB (Brox, Walker).pdf |
| 481 | BGB Allgemeiner Teil (Burkhard Boemke, Bernhard Ulrici (auth.).pdf |
| 712 | BGB Allgemeiner Teil (Detlef Leenen, Martin Häublein).pdf |
| 910 | BGB-Schuldrecht Besonderer Teil, eBook.pdf |
| 1160 | Band I (§§1-240).pdf |
| 620 | Behm, Berger, Вещное право.pdf |
| 408 | Bengel_Simmerding_Grundbuch_Grundstueck_Grenze.pdf |
| 521 | Besonderes Schuldrecht (Jan Dirk Harke (auth.)).pdf |
| 481 | Boemke_Ulrici_BGB_Allgemeiner_Teil.pdf |
| 1509 | Bork & Schäfer - GmbHG Kommentar (2015).pdf |
| 620 | Brehm_Berger_Sachenrecht.pdf |
| 575 | Brox, Walker, Allgemeines Schuldrecht.pdf |
| 451 | Brox_Walker_Allgemeiner_Teil_des_BGB.pdf |
| 575 | Brox_Walker_Allgemeines_Schuldrecht.pdf |
| 136 | Eckpfeiler (сделка).pdf |
| 1204 | Eckpfeiler_Des_Zivilrechts_J_Von_Staudingers_Kommentar_Zum_Buergerlichen.pdf |
| 1204 | Eckpfeiler_Des_Zivilrechts_Von_Staudingers_Kommentar_Zum_BGB.pdf |
| 529 | Emmerich-Sonnenschein Miete 10 Aufl.pdf |
| 910 | Emmerich_BGB_Schuldrecht_Besonderer_Teil.pdf |
| 1142 | Fikentscher,_Heinemann,_Schuldrecht_Allgemeiner_und_Besonderer_Teil.pdf |
| 1142 | Fikentscher_Heinemann_Schuldrecht.pdf |
| 999 | Flume, Das Rechtsgeschäft.pdf |
| 3047 | GESELLSCHAFTSRECHT BGB HGB PARTGG GMBHG AKTG UMWG GENG (Henssler, Strohn).pdf |
| 436 | Gernhuber, Das Schuldverhaltnis.pdf |
| 596 | Gernhuber, Die Erfullung und ihre Surrogate.pdf |
| 2114 | GmbH-Gesetz Kommentar ( etc.).pdf |
| 212 | Gursky_Sachenrecht.pdf |
| 2564 | HGB Kommentar (Handelsstand, Handelsgesellschaften).pdf |
| 447 | Hanns_Prütting_Вещное право_37.Auflage.pdf |
| 511 | Harke, Allgemeines Schuldrecht.pdf |
| 511 | Harke_Allgemeines_Schuldrecht.pdf |
| 632 | Hubner_Allgemeiner_Teil_des_Burgerlichen_Gesetzbuches.pdf |
| 1500 | Insolvenzordnung Band 1 §§ 1-55.pdf |
| 908 | Insolvenzordnung Band 3 §§ 103-128 (Giesen, Jacoby).pdf |
| 370 | Insolvenzordnung Band 5.1 §§ 148-155 (Eckardt, Fehrenbacher).pdf |
| 1569 | Insolvenzordnung Band 5.2 §§ 156-173 (Eckardt).pdf |
| 565 | Insolvenzordnung Band 6 §§ 174-216 (Henckel, Gerhardt).pdf |
| 1368 | Insolvenzordnung Band 7 §§ 217-285.pdf |
| 944 | Insolvenzordnung Band 9 §§ 335-359.pdf |
| 3419 | Kommentar zum BGB Band 1 §§ 1-610 (Bamberger, Roth).pdf |
| 1292 | Kuntze_Grundbuchrecht_Kommentar.pdf |
| 502 | Larenz - Wolf - Allgemeiner Teil des Burgerlichen Rechts.pdf |
| 560 | Larenz_Lehrbuch_des_Schuldrechts_II_Besonderer_Teil.pdf |
| 686 | Larenz_Lehrbuch_des_Schuldrechts_I_Allgemeiner_Teil.pdf |
| 522 | Leenen_BGB_Allgemeiner_Teil_Rechtsgeschaftslehre.pdf |
| 712 | Leenen_Haublein_BGB_Allgemeiner_Teil.pdf |
| 568 | Leipold_BGB_I_Einfuhrung_und_allgemeiner_Teil.pdf |
| 310 | Lohning - Schuldrecht_ii_besonderer.pdf |
| 627 | Looschelders, Schuldrecht. Allgemeiner Teil.pdf |
| 627 | Looschelders_Schuldrecht._Allgemeiner_Teil.pdf |
| 301 | Meder_Czelk_Grundwissen_Sachenrecht.pdf |
| 277 | Medicus_D_Allgemeiner_Teil_des_BGB_10_Aufl__2010.pdf |
| 449 | Medicus_Lorenz_Schuldrecht_I._Allgemeiner_Teil.pdf |
| 570 | Medicus_Lorenz_Schuldrecht_II._Besonderer_Teil.pdf |
| 2416 | Münchener Kommentar zur Insolvenzordnung-Band1.§§1-79.pdf |
| 332 | Münchener Kommentar zur Insolvenzordnung-Band1.§§217-359.pdf |
| 803 | Neuner - Allgemeiner Teil des Bürgerlichen Rechts (перевод).pdf |
| 803 | Neuner_Allgemeiner_Teil_des_Burgerlichen_Rechts.pdf |
| 1166 | Oechsler - Обязательственное право.pdf |
| 965 | Oetker + Maultzsch - Договорное право - особенная часть (2018, 5 издание).pdf |
| 340 | Rehberg_BGB_AT.pdf |
| 475 | Reich_Schmitz_Einfuhrung_in_das_Burgerliche_Recht.pdf |
| 528 | Sachenrecht (Hans Josef Wieling) - немецкий язык.pdf |
| 709 | Schuldrecht. Besonderer Teil. 16. Auflage_en.pdf |
| 570 | Stadler, Allgemeiner Teil des BGB.pdf |
| 570 | Stadler_Allgemeiner_Teil_des_BGB.pdf |
| 1204 | Staudingers_Kommentar_zum_Burgerlichen_Gesetzbuch.pdf |
| 7824 | Westermann, Grunewald, Maier-Reimer - BGB Kommentar (2023).pdf |
| 20 | Wieling_Die_Grundbucheintragung.pdf |
| 529 | Wieling_Sachenrecht.pdf |
| 1113 | Wilhelm, Sachenrecht.pdf |
| 1540 | Wilhelm_Sachenrecht_7_Aufl..pdf |
| 10 | Wilsch_The_German_Grundbuchordnung.pdf |
| 508 | Wolf, Вещное право 21 Aufl.pdf |
| 508 | Wolf_Sachenrecht_21_Aufl.pdf |
| 869 | ZPO Kommentar Band 1 Einleitung §§ 1-49.pdf |
| 1116 | ZPO Kommentar Band 6 §§ 355-510c.pdf |
| 584 | ZPO Kommentar Band 8 §§ 592-723.pdf |
| 742 | ZPO Kommentar Band 9 §§ 724-802l.pdf |
| 619 | Zerres_Burgerliches_Recht.pdf |
| 449 | Медикус Лоренц - Общая часть обязат права.pdf |
| 570 | Медикус и Лоренц - Особенная часть обязат права.pdf |

Всего страниц: 109410


### Как устроена распечатка beck-online (MüKo)

Файл — не книга, а **склейка заданий печати**. Каждое задание: шапка
(`BGB § 273 Zurückbehaltungsrecht Krüger Münchener Kommentar zum BGB /
10. Auflage 2025`), текст, аппарат сносок, строка
`Zitiervorschläge: MüKoBGB/Krüger, 10. Aufl. 2025, BGB § 273 Rn. 9-12`.
Отсюда берётся всё, что нужно для ссылки: параграф, комментатор (у каждого §
свой), издание, год и охваченные Randnummern.

Разметка страницы (A4, 595×842):

| что | признак |
|---|---|
| колонтитул | `14/02/2026, 19:33 § 104 Geschäftsunfähigkeit - beck-online`, top 16,5 |
| водяной знак | `KopievonPeacePalaceLibrary…` без пробелов, top 1,6 |
| тело | x0 69,9–70,2, кегль 10,5–11,0 |
| **Randnummer** | последнее слово строки, x0 ≈ 431, x1 = 441,5 — ЗА полосой набора |
| заголовок | x0 43,7, кегль 12,0 |
| номер сноски в аппарате | x0 43,7, кегль 8,4 |
| текст сноски | x0 58,7, кегль 10,5 |
| колонцифра | ЕЁ НЕТ: это распечатка с экрана, а не книга |
| подвал | ссылка `https://beck-online…/Print/CurrentDoc?vpath=…` |

Отсюда конвейер: единица — `mode: margin`, side right, `x_range` около
[425, 450]; параграф — из колонтитула через `address.section.mode: pattern`;
аппарат отделяется не по высоте, а **по x0**: 55–65 — сноска, ≥68 — тело.
Кегль 8,4 при x0 ≈ 43,7 — номер сноски, кегль 12,0 при том же x0 — заголовок,
и путать их нельзя.

Нумерация сносок сквозная **по параграфу**, а не по странице, и сам аппарат
стоит отдельными страницами в конце задания. Стандартный `attach_notes`
раздаёт сноски постранично и здесь промахнётся: сноску придётся привязывать
к карточке по паре (§, номер) — тем же приёмом, что применялся к
французским электронным изданиям Dalloz.

Колонцифры нет вовсе, поэтому `strip_junk` будет вслепую резать голые числа
в верхней и нижней полосе — а номер сноски в аппарате как раз голое число и
стоит вверху страницы. Полосу придётся отключить или сузить.

### Что делать с наложением диапазонов

Файлы названы по диапазонам параграфов, и диапазоны налезают друг на друга:
`104-144` перекрывает пять отдельных файлов, `145-185` — три, `985-1011` и
`986-1011` — один и тот же кусок в двух скачиваниях. По имени судить нельзя;
покрытие снимается со строк `Zitiervorschläge` (скрипт
`de-pdf/inv/muko_cover.py`).

Решение принято такое: **MüKoBGB 10. Aufl. 2025 — это ОДИН источник**, а не
двадцать четыре. Файлы — лишь распечатки его частей. Карточка адресуется
парой (§, Rn), и повторы схлопываются сами: у двух распечаток одного § и
одного Rn совпадёт `external_id`.
